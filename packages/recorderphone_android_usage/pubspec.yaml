name: recorderphone_android_usage
description: Android UsageStats helpers for RecorderPhone (Usage Access / aggregated usage).
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.recorderphone.android_usage
        pluginClass: RecorderphoneAndroidUsagePlugin

